server:
  port: 8080
  servlet:
    context-path: /duel

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/duel_dev?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/**/*.xml